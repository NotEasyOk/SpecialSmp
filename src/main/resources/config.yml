# ======================================================
#          SPECIAL SMP - CARDS CONFIGURATION
# ======================================================
# Version: 1.21x
#Author: NotEasyOk
# ------------------------------------------------------
# CARD COOLDOWNS (In Seconds)
# ------------------------------------------------------
cards:
  ultimate:
    sword_cooldown: 120
  warden:
    left_click_cooldown: 60
    right_click_cooldown: 120
    shift_click_cooldown: 60
  zombie:
    cooldown: 60
  ruin:
    silverfish_cooldown: 30
    poison_cooldown: 45
  nothing:
    left_click_cooldown: 20
    right_click_cooldown: 40
    shift_click_cooldown: 60
  lighting:
    left_click_cooldown: 40
    right_click_cooldown: 20
    shift_click_cooldown: 60
  herobrine:
    left_click_cooldown: 10
    right_click_cooldown: 60
    shift_click_cooldown: 180
  ghost:
    left_click_cooldown: 60
    right_click_cooldown: 40
    shift_click_cooldown: 90
  enderman:
    teleport_cooldown: 60
    pull_cooldown: 40
    breath_cooldown: 90
  creeper:
    left_click_cooldown: 60
    right_click_cooldown: 90
    shift_click_cooldown: 120

# ------------------------------------------------------
# CARD SPECIFIC ABILITIES
# ------------------------------------------------------
zombie-card:
  max-zombies: 2
  duration-seconds: 60

# ------------------------------------------------------
# MESSAGES
# ------------------------------------------------------
messages:
  prefix: "§8[§6§lSpecialSMP§8] §r"
  cooldown-active: "§cPlease wait §e%time%s §cbefore using this again."

# ------------------------------------------------------
# OTHER SETTINGS
# ------------------------------------------------------
first-join:
  give-random-card: true
  allowed-cards:
    - "Enderman"
    - "Herobrine"
    - "Nothing"
    - "Zombie"
    - "Warden"
    - "Creeper"
    - "Lighting"
    - "Ghost"
    - "Ruin"
